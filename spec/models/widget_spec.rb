require 'rails_helper'

RSpec.describe Widget, type: :model do
  it "ensures that JustNeph is pretty awesome" do
    expect(true).to be_truthy
  end

  it "verifies that he is indeed not modest either" do
    expect(true).to be_truthy
  end
end
